# Rust
/target
**/*.rs.bk
Cargo.lock.orig

# Local state / secrets
*.db
*.db-journal
.env
.env.*
config.local.toml

# macOS
.DS_Store

# In-house docs — kept local, deliberately not part of the public repo.
# (See the publish decision: docs/ + STATUS + CLAUDE.md are working-tree only.)
# AGENTS.md is a local symlink to CLAUDE.md (one orientation doc, two names).
/docs/
/CLAUDE.md
/AGENTS.md

# Haven local state — backlog graph/content + run logs live under the user's
# ~/.haven; these repo-local projections/markers are disposable and never public.
/_haven/
/.haven-project
/.haven-run/
